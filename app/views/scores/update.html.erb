
<turbo-stream action="replace" target="User-<%= user.id %>">
  <template>
    <% game.users_and_data(user.id).each do |user, score, cards| %>
      <%= render "user", game: game, user: user, score: score, cards: cards, visible: true %>
    <% end %>
  </template>
</turbo-stream>
