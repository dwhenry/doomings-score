<%= e.hidden_field :type %>

<div class="flex bg-green-200 border-b border-gray-700">
  <div class="font-bolder py-2 px-5 bg-green-500">
    EFFECTS:
  </div>
  <div class="py-2 px-5 <%= "bg-red-200 font-red-900" if effect == "none" %>">
    <%= link_to "None", admin_card_effect_url(card, effect: "none") %>
  </div>
  <div class="py-2 px-5 <%= "bg-red-200 font-red-900" if effect == "effect_count" %>">
    <%= link_to "Effect Count", admin_card_effect_url(card, effect: "effect_count") %>
  </div>
  <div class="py-2 px-5 <%= "bg-red-200 font-red-900" if effect == "trait_count" %>">
    <%= link_to "Trait Count", admin_card_effect_url(card, effect: "trait_count") %>
  </div>
</div>
